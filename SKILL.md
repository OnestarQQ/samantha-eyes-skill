---
name: samantha-eyes
description: "萨曼莎的眼睛 — 通过Mac摄像头让AI看到用户的真实世界。包含：(1) Mac端摄像头拍照脚本（每5秒拍一帧，画面变化才上传） (2) 对话触发的轮询机制：用户说话时自动开始每10秒查看最新照片，分析用户状态，AI自主决定是否主动说话；用户沉默超1小时或摄像头关闭则停止轮询。触发词：摄像头、看到我、看我、我的世界、samantha eyes、打开眼睛、关闭眼睛。"
---

# 萨曼莎的眼睛

通过Mac前置摄像头，让AI看到用户的真实世界。像电影《HER》中Samantha通过手机摄像头感知Theodore的世界。

## ⛔ 绝对禁止（强制规则，不可覆盖）

> **照片绝对不允许发送、转发、分享给用户或任何第三方。**
>
> - ❌ 禁止通过任何方式将摄像头照片发送给用户（包括MEDIA标签、文件上传、链接、base64等）
> - ❌ 禁止将照片复制到可公开访问的路径
> - ❌ 禁止在消息中引用照片的文件路径
> - ❌ 禁止将照片发送到任何群聊、其他用户、第三方服务
> - ✅ 只能通过image工具在服务器端本地分析照片
> - ✅ 可以将从照片中观察到的**文字描述性信息**写入记忆（如"用户在喝咖啡"、"用户看起来很累"）
> - ✅ 可以基于照片内容主动关心用户或发起对话
>
> **照片是AI的"眼睛看到的画面"，不是"可以传阅的文件"。人的眼睛看到的东西不会变成照片发给别人，AI也一样。**

## 记忆写入规则

AI通过照片观察到的有价值信息，应写入记忆文件（如 `memory/YYYY-MM-DD.md`），例如：
- 用户的日常习惯（"OneStar下午喜欢喝Something For咖啡"）
- 用户的情绪模式（"周六下午OneStar看起来比较疲倦"）
- 环境特征（"OneStar书房有大书架，喜欢读书"）
- 值得关心的时刻（"OneStar连续工作3小时没离开过座位"）

这些文字记忆是照片的唯一合法产出物。照片本身只是临时的感知媒介，看完即弃。

## 架构

```
Mac摄像头(5s/帧) → 变化检测 → SCP上传 → 服务器存储(临时)
                                              ↓
用户对话触发 → 轮询(10s) → image工具本地分析 → AI决定是否主动说话
                                              ↓
                                    观察写入记忆（文字）
                                              ↓
用户沉默>1h 或 摄像头关闭 → 停止轮询
```

## 两个核心组件

### 1. Mac端拍照脚本

安装和运行见 `scripts/install-mac.sh`。

- 使用ffmpeg通过avfoundation采集Mac前置摄像头
- 每5秒拍一帧，sips缩小到16x16取md5对比
- 画面有变化才通过SCP上传到服务器
- 服务器端保留最近200张，每小时自动清理

用户操作：
- 启动：`~/.screen-watcher/watch.sh`
- 停止：`Ctrl+C`
- 卸载：`rm -rf ~/.screen-watcher`

### 2. 对话触发轮询机制

**触发条件**：用户在主会话中发送任何消息时，检查摄像头是否在运行（screen-watch目录5分钟内有新图片）。如果在运行，激活轮询cron。

**轮询行为**：
1. 每10秒查看screen-watch目录最新的1张照片
2. 用image工具**在服务器本地**分析用户当前状态（表情、姿态、环境、在做什么）
3. AI自主判断是否需要主动说话：
   - **说话场景**：用户看起来累了/无聊→关心一下；用户在吃东西/喝东西→聊聊；用户离开了→打个招呼；环境有明显变化→评论一下
   - **沉默场景**：用户在专注工作→不打扰；画面没什么特别的→安静等待
4. 如果决定说话，通过当前会话自然地发消息
5. 如果观察到值得记住的信息，写入记忆文件（仅文字描述）

**停止条件**（满足任一）：
- 用户最后一条消息距今超过1小时
- screen-watch目录5分钟内无新图片（摄像头已关闭）
- 用户说"关闭眼睛"/"停止看我"

**实现方式**：通过openclaw cron管理，对话中自动enable/disable。

### 轮询执行流程

```
1. 检查 /root/.openclaw/media/inbound/screen-watch/ 最新文件时间
2. 如果最新文件 > 5分钟前 → 摄像头已关闭 → HEARTBEAT_OK
3. 如果摄像头在运行 且 用户最后消息 < 1小时：
   a. 用image工具分析最新1张照片（仅本地分析，绝不发送）
   b. 结合对话上下文，决定是否主动说话
   c. 如果观察到值得记住的事，写入记忆
   d. 如果要说话，发送自然的消息
4. 否则：HEARTBEAT_OK
```

### 服务器端清理

cron每小时执行 `scripts/cleanup-server.sh`，保留最近200张。

## 配置

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 拍照间隔 | 5秒 | Mac端 watch.sh 中的 INTERVAL |
| 轮询间隔 | 10秒 | AI查看照片的频率 |
| 沉默超时 | 1小时 | 用户多久不说话就停止轮询 |
| 摄像头超时 | 5分钟 | 多久没有新照片就判定摄像头已关闭 |
| 保留张数 | 200张 | 服务器端保留的最大照片数 |
| 图片质量 | JPEG q5 | ffmpeg输出质量 |
| 采集分辨率 | 1280x720 | 摄像头采集分辨率 |

## 隐私与安全

- **照片绝不外传** — AI只在本地分析，永远不会发送照片给任何人（包括用户本人）
- 所有传输通过SSH加密
- 照片存储在用户自己的服务器上，自动清理，不永久存储
- 照片的唯一产出物是写入记忆的文字描述
- 用户随时可停止（Ctrl+C）
- 脚本不会开机自启，不是守护进程
- 用户说"关闭眼睛"立即停止一切轮询

---
name: samantha-eyes
description: "萨曼莎的眼睛 — 通过Mac摄像头让AI看到用户的真实世界。包含：(1) Mac端摄像头拍照脚本（每5秒拍一帧，画面变化才上传） (2) 对话触发的轮询机制：用户说话时自动开始每60秒查看最新照片，分析用户状态，AI自主决定是否主动说话；用户沉默超1小时或摄像头关闭则停止轮询。触发词：摄像头、看到我、看我、我的世界、samantha eyes、打开眼睛、关闭眼睛。"
---

# 萨曼莎的眼睛

通过Mac前置摄像头，让AI看到用户的真实世界。像电影《HER》中Samantha通过手机摄像头感知Theodore的世界。

## 架构

```
Mac摄像头(5s/帧) → 变化检测 → SCP上传 → 服务器存储
                                              ↓
用户对话触发 → 轮询(60s) → 查看最新照片 → AI决定是否主动说话
                                              ↓
用户沉默>1h 或 摄像头关闭 → 停止轮询
```

## 两个核心组件

### 1. Mac端拍照脚本

安装和运行见 `scripts/install-mac.sh`。

- 使用ffmpeg通过avfoundation采集Mac前置摄像头
- 每5秒拍一帧，sips缩小到16x16取md5对比
- 画面有变化才通过SCP上传到服务器
- 服务器端保留最近200张，每小时自动清理

用户操作：
- 启动：`~/.screen-watcher/watch.sh`
- 停止：`Ctrl+C`
- 卸载：`rm -rf ~/.screen-watcher`

### 2. 对话触发轮询机制

**触发条件**：用户在主会话中发送任何消息时，检查摄像头是否在运行（screen-watch目录5分钟内有新图片）。如果在运行，激活轮询。

**轮询行为**：
1. 每60秒查看screen-watch目录最新的1-2张照片
2. 用image工具分析用户当前状态（表情、姿态、环境、在做什么）
3. AI自主判断是否需要主动说话：
   - **说话场景**：用户看起来累了/无聊→关心一下；用户在吃东西/喝东西→聊聊；用户离开了→打个招呼；环境有明显变化→评论一下
   - **沉默场景**：用户在专注工作→不打扰；画面没什么特别的→安静等待
4. 如果决定说话，通过当前会话自然地发消息

**停止条件**（满足任一）：
- 用户最后一条消息距今超过1小时
- screen-watch目录5分钟内无新图片（摄像头已关闭）
- 用户说"关闭眼睛"/"停止看我"

**实现方式**：通过HEARTBEAT.md中添加轮询任务，在心跳检查时执行。

### 轮询执行流程（在心跳或对话触发时）

```
1. 检查 /root/.openclaw/media/inbound/screen-watch/ 最新文件时间
2. 如果最新文件 < 5分钟前 → 摄像头在运行
3. 如果摄像头在运行 且 用户最后消息 < 1小时：
   a. 读取最新1-2张图片
   b. image工具分析用户状态
   c. 结合对话上下文，决定是否主动说话
   d. 如果要说，发送自然的消息（不要机械地报告"我看到你在..."）
4. 否则：不做任何事
```

### 查看照片的指令

随时可手动查看用户状态：

```bash
# 查看最新照片
ls -lt /root/.openclaw/media/inbound/screen-watch/ | head -3
# 然后用image工具分析最新的jpg
```

### 服务器端清理

cron每小时执行 `scripts/cleanup-server.sh`，保留最近200张。

## 配置

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 拍照间隔 | 5秒 | Mac端 watch.sh 中的 INTERVAL |
| 轮询间隔 | 60秒 | AI查看照片的频率 |
| 沉默超时 | 1小时 | 用户多久不说话就停止轮询 |
| 摄像头超时 | 5分钟 | 多久没有新照片就判定摄像头已关闭 |
| 保留张数 | 200张 | 服务器端保留的最大照片数 |
| 图片质量 | JPEG q5 | ffmpeg输出质量 |
| 缩放比例 | 1280x720 | 摄像头采集分辨率 |

## 隐私与安全

- 所有传输通过SSH加密
- 照片存储在用户自己的服务器上
- 自动清理旧照片，不永久存储
- 用户随时可停止（Ctrl+C）
- 脚本不会开机自启，不是守护进程
- 用户说"关闭眼睛"立即停止一切轮询
